# Managed by Doors DNS selector: systemd-resolved-compat
# Keep NetworkManager per-link DNS integration so DHCP, captive portals, and
# VPN-provided resolvers continue to work on first boot. A strict mode replaces
# this file when the owner explicitly runs `ujust dns-selector <strict-mode>`.
[main]
dns=systemd-resolved
systemd-resolved=true
