# Make per-user mise shims visible to the systemd user manager, so desktop
# launchers, terminals started by the session, and user services find the
# agent CLIs without shell activation.
PATH=${HOME}/.local/share/mise/shims:${PATH}
