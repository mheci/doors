# Doors — first-login Hyprland config (skeleton, copied to new users)
# Pulls Doors defaults + colors, then local overrides live here.
# NOTE: uncomment and adjust the monitor line for your display.
source = /usr/share/hypr/doors/doors.conf

# monitor = ,preferred,auto,1

# wallpaper (edit path as needed)
# exec-once = hyprpaper

# autostart
# exec-once = waybar
# exec-once = cliphist wipe
